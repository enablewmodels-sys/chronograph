{
  "status": "passed",
  "presets": 28,
  "checks": [
    "asset exact bits, shape checks, deduplication and read scope",
    "28 registry presets: migration → ingest → exact source export → retry/conflict",
    "invalid records and missing assets cannot advance checkpoints",
    "official MCP client discovers connector contracts",
    "SIGKILL restart and independent backup restore preserve every preset, asset and receipt"
  ]
}
