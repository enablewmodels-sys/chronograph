# DNS must resolve to the host; use a hostname without scheme, path or trailing slash.
DOMAIN=graph.example.com
ACME_EMAIL=operator@example.com
CHRONOGRAPH_TAG=local
# All indexes are memory-resident. Size this for your actual graph and backup workload.
CHRONOGRAPH_MEMORY=2g
